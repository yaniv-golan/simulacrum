import {createBrowserEvidence} from './browser-evidence.mjs';
import {chromium} from 'playwright';
import assert from 'node:assert/strict';
import {mkdirSync,writeFileSync} from 'node:fs';
const browserEvidence=createBrowserEvidence();

const browser=await chromium.launch(),page=await browser.newPage({viewport:{width:1280,height:720}}),errors=[];
page.setDefaultTimeout(6000);
page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});page.on('requestfailed',r=>errors.push(r.url()));page.on('response',r=>{if(r.status()>=400)errors.push(`${r.status()} ${r.url()}`);});
const out=process.argv[3]??'artifacts/inspector-browser';mkdirSync(out,{recursive:true});
const read=()=>page.evaluate(()=>window.workshopProbe.observe().frames[0].metadata.blueprint);
const selectPart=async name=>{if(await page.locator('.machine-picker').count()&&!await page.locator('.machine-picker').evaluate(el=>el.open))await page.locator('.machine-picker > summary').click();await page.locator('.part-list-item').filter({hasText:new RegExp(`^${name}$`)}).click();};
const withinInspector=async locator=>locator.evaluateAll(elements=>elements.map(element=>{const box=element.getBoundingClientRect(),panel=element.closest('.inspector-panel').getBoundingClientRect();return {text:element.textContent,visible:!!element.getClientRects().length&&box.top>=panel.top&&box.bottom<=panel.bottom&&box.left>=panel.left&&box.right<=panel.right};}));
let build;
try{
 await browserEvidence.goto(page,process.argv[2]??'http://127.0.0.1:4173/');await page.waitForFunction(()=>window.workshopProbe);build=await page.locator('meta[name=build-id]').getAttribute('content');
 await page.locator('[data-command=start-guide]').click();for(let i=0;i<16;i++){await page.locator('[data-command=guide-step]').click();if(i===7){await page.screenshot({path:`${out}/guide-step-eight.png`});const label=await page.locator('.selection-label').boundingBox(),toolbar=await page.locator('.selection-actions').boundingBox();assert.ok(label&&toolbar,'guided selection exposes its label and toolbar');assert.ok(label.x+label.width<=toolbar.x||toolbar.x+toolbar.width<=label.x||label.y+label.height<=toolbar.y||toolbar.y+toolbar.height<=label.y,`selection label overlaps toolbar: ${JSON.stringify({label,toolbar})}`);}}await page.getByRole('button',{name:'Leave guide',exact:true}).click();await selectPart('Motor');
 const duty=page.getByRole('spinbutton',{name:'Drive setting',exact:true});assert.equal(await duty.isVisible(),true,'motor power must be a visible primary control without opening engineering details');
 assert.equal(await page.locator('.part-settings').evaluate(el=>el.open),false);assert.equal(await page.locator('.part-settings > summary').textContent(),'Engineering details');
 const compact=await withinInspector(page.locator('.port-button,.part-settings > summary,[aria-label="Drive setting"]'));assert.equal(await page.locator('.port-button').count(),3);assert.equal(await page.locator('.port-button[data-port-id=mount]').count(),0);assert.match(await page.locator('.mount-relationship').innerText(),/Bolted to Chassis/);assert.equal(await page.getByRole('button',{name:'Adjust mount',exact:true}).isVisible(),true);assert.equal(await page.getByRole('button',{name:'Detach',exact:true}).isVisible(),true);assert.ok(compact.every(row=>row.visible),`primary control, three ports and Engineering details must fit at 1280×720: ${JSON.stringify(compact)}`);
 await page.screenshot({path:`${out}/motor-primary.png`});
 const strength=page.getByRole('slider',{name:'Drive strength',exact:true});assert.equal(await strength.isVisible(),true);await strength.press('Home');await strength.press('ArrowRight');assert.equal((await read()).parts.find(p=>p.name==='Motor').parameters.defaultDuty,-.95);await page.locator('.drive-directions').getByRole('button',{name:'Reverse',exact:true}).click();assert.equal((await read()).parts.find(p=>p.name==='Motor').parameters.defaultDuty,-1);await page.locator('.drive-directions').getByRole('button',{name:'Forward',exact:true}).click();assert.equal((await read()).parts.find(p=>p.name==='Motor').parameters.defaultDuty,1);
 const before=await read();
 await page.locator('.port-button[data-port-id=power]').click();
 assert.equal(await page.locator('.port-list > .port-button[data-port-id=power] + .port-explanation').count(),1,'port actions belong immediately after their port');
 assert.equal(await page.locator('.port-explanation [data-disconnect-id]').count(),1);
 assert.equal(await page.locator('.port-explanation').evaluate(el=>!!(el.compareDocumentPosition(document.querySelector('.placement-settings'))&Node.DOCUMENT_POSITION_FOLLOWING)),true,'port actions precede position editing');
 assert.deepEqual(await read(),before,'opening an occupied power port only inspects');
 await page.getByRole('button',{name:'Cancel connection',exact:true}).click();assert.equal(await page.locator('.port-explanation').count(),0);assert.ok((await page.locator('[data-live-part]').textContent()).trim().length>0,'cancel preserves populated live status');assert.deepEqual(await read(),before);
 await page.locator('[data-command=run]').click();await page.waitForFunction(()=>window.workshopProbe.observe().cursor.tick>=10);await page.locator('[data-command=pause]').click();const paused=await read();
 for(const port of await page.locator('.port-button').all()){assert.equal(await port.isEnabled(),true,'paused ports remain inspectable');}
 await page.locator('.port-button[data-port-id=power]').click();assert.equal(await page.locator('.port-explanation [data-disconnect-id]').isEnabled(),false);assert.equal(await page.locator('.target-button').count(),0);assert.deepEqual(await read(),paused,'paused inspection cannot mutate blueprint');await page.screenshot({path:`${out}/paused-port.png`});
 await page.locator('.port-explanation').getByRole('button',{name:'Return to Build',exact:true}).click();assert.equal(await duty.isEnabled(),true);
 await selectPart('Drive wheel');assert.equal(await page.locator('.port-button').first().getAttribute('data-port-id'),'axle');assert.equal(await page.locator('.port-button[data-port-id=mount]').count(),0,'wheel mounting is a surface action, never an optional fixed socket');await page.locator('.port-button[data-port-id=axle]').click();assert.match(await page.locator('.port-explanation').textContent(),/holds the wheel too; no separate fixed mount/);await page.screenshot({path:`${out}/wheel-axle.png`});
 await page.locator('.more-parts > summary').click();await page.locator('[data-part-type=commandReceiver]').click();const receiver=(await read()).parts.find(part=>part.type==='commandReceiver');assert.ok(receiver);
 await selectPart('Motor');await page.locator('.port-button[data-port-id=signal]').click();await page.locator(`.target-button[data-target-part-id="${receiver.id}"]`).click();const connected=await read(),motor=connected.parts.find(part=>part.name==='Motor');assert.ok(connected.connections.some(connection=>connection.kind==='signal'&&connection.a.part===receiver.id&&connection.a.port==='signal'&&connection.b.part===motor.id&&connection.b.port==='signal'),'selecting a signal source actually connects output to motor input');assert.equal(await page.getByRole('spinbutton',{name:'Drive setting',exact:true}).isVisible(),false,'connected signal replaces primary default drive');assert.match(await page.locator('.signal-ownership').textContent(),/Drive commanded by/);assert.equal(await page.locator('.signal-ownership .part-link').textContent(),receiver.name);await page.screenshot({path:`${out}/signal-owner.png`});await page.locator('.signal-ownership .part-link').click();assert.equal(await page.locator('.selected-part-header h2').textContent(),receiver.name,'ownership link selects actual signal source');assert.deepEqual(await page.locator('.port-button.signal .port-title').allTextContents(),['Control input','Control output']);assert.equal(await page.locator('.drive-buttons button.primary').count(),0,'receiver action must not imply an active drive command');
 assert.deepEqual(errors,[]);browserEvidence.assertUnchanged();writeFileSync(`${out}/result.json`,JSON.stringify({...browserEvidence.identity,build,errors,checks:['primary motor power and all ports fit without scrolling at 1280x720','port actions inline before placement','paused ports inspect without mutation and return to Build','cancel retains populated live status','wheel axle first without duplicate fixed socket','actual signal source replaces default drive and can be inspected']},null,2));console.log('inspector browser passed');
}catch(error){await page.screenshot({path:`${out}/failed.png`}).catch(()=>{});writeFileSync(`${out}/failure.json`,JSON.stringify({build,errors,message:error.message},null,2));throw error;}finally{try{browserEvidence.assertUnchanged();}finally{await browser.close();}}
