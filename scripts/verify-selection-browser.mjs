import {chromium} from 'playwright';
import assert from 'node:assert/strict';
import {mkdirSync,writeFileSync} from 'node:fs';
const browser=await chromium.launch(),page=await browser.newPage({viewport:{width:1280,height:720}}),errors=[];
page.on('pageerror',e=>errors.push(e.message));page.on('console',m=>{if(m.type()==='error')errors.push(m.text());});page.on('requestfailed',r=>errors.push(r.url()));
mkdirSync('artifacts/selection-ux',{recursive:true});
try{
 await page.goto(process.argv[2]??'http://127.0.0.1:4173/');await page.locator('[data-command=start-guide]').click();for(let i=0;i<4;i++)await page.locator('[data-command=guide-step]').click();await page.getByRole('button',{name:'Leave guide',exact:true}).click();
 await page.mouse.click(672,416);assert.equal(await page.locator('.part-list-item.selected').textContent(),'Motor','visible motor casing must pick motor, not wheel decoration');
 await page.mouse.click(866,551);assert.equal(await page.locator('.part-list-item.selected').count(),0,'empty floor clears selection');
 if(!await page.locator('.machine-picker').evaluate(el=>el.open))await page.locator('.machine-picker > summary').click();await page.locator('.part-list-item').filter({hasText:/^Motor$/}).click();await page.locator('.part-settings summary').click();await page.getByRole('spinbutton',{name:'Drive setting',exact:true}).fill('0.5');await page.getByRole('spinbutton',{name:'Drive setting',exact:true}).press('Tab');
 await page.waitForFunction(()=>window.workshopProbe.observe().frames[0].metadata.blueprint.parts.find(p=>p.name==='Motor').parameters.defaultDuty===.5);
 assert.equal(await page.locator('.part-settings').getAttribute('open'),'');await page.getByRole('combobox',{name:'Material',exact:true}).selectOption('aluminium');await page.waitForFunction(()=>window.workshopProbe.observe().frames[0].metadata.blueprint.parts.find(p=>p.name==='Motor').authoredMaterial.body==='aluminium');assert.equal(await page.locator('.part-settings').getAttribute('open'),'');assert.equal(await page.getByRole('spinbutton',{name:'Drive setting',exact:true}).inputValue(),'0.5');
 await page.locator('[data-command=new]').click();await page.locator('[data-command=start-guide]').click();for(let i=0;i<16;i++)await page.locator('[data-command=guide-step]').click();
 if(!await page.locator('.machine-picker').evaluate(el=>el.open))await page.locator('.machine-picker > summary').click();await page.locator('.part-list-item').filter({hasText:/^Motor$/}).click();await page.locator('.port-button[data-port-id=power]').click();await page.getByRole('button',{name:'Disconnect Power · Cell',exact:true}).click();if(await page.locator('.port-button[data-port-id=power]').getAttribute('aria-expanded')!=='true')await page.locator('.port-button[data-port-id=power]').click();await page.getByRole('button',{name:'Wire Cell · power (parts stay put)',exact:true}).click();assert.equal(await page.locator('.guide-progress').textContent(),'16 / 16 steps');
 await page.getByRole('button',{name:'Clear selection',exact:true}).click();await page.screenshot({path:'artifacts/selection-ux/repaired.png'});assert.deepEqual(errors,[]);writeFileSync('artifacts/selection-ux/result.json',JSON.stringify({build:await page.locator('meta[name=build-id]').getAttribute('content'),errors,checks:['motor picking','empty-space deselection','settings stay open','property persists after material edit','manual reversed wiring satisfies guide']},null,2));console.log('selection and manual wiring browser regressions passed');
}finally{await browser.close();}
